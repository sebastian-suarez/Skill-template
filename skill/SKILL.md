---
name: word-counter
description: >
  Count words, characters, and sentences in text. Use whenever the user
  asks to count words, character count, sentence count, or text statistics.
---

# Word counter

Run the script with the text as arguments:

\```bash
node <SKILL_PATH>/scripts/index.js [text to count]
\```

The script outputs JSON with `words`, `chars`, and `sentences`.
Present the results to the user in a readable format.
